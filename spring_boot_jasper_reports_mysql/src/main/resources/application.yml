spring:
    jpa:
        database: MYSQL
        hibernate:
            ddl-auto: validate

    datasource:
        url: jdbc:mysql://127.0.0.1:3306/spring_jasper?useSSL=false
        username: spring
        password: boot
        driver-class-name: com.mysql.jdbc.Driver